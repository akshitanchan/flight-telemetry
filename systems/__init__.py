"""Systems layer: replay ingestion and pluggable spatiotemporal index benchmark.

Marks ``systems`` as a regular package so ``from systems.xxx import ...``
resolves without relying on implicit namespace-package behaviour or sys.path hacks.
"""
