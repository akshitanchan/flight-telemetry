"""ML layer: PRC 2025 fuel-burn baseline, feature extraction, training, and serving.

Marks ``ml`` as a regular package so ``from ml.xxx import ...`` resolves
without relying on implicit namespace-package behaviour or sys.path hacks.
"""
