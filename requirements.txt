# Flight Telemetry Platform — Python dependencies
# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Schema validation
jsonschema>=4.20

# Spatial indexing (bronze-to-silver transform)
h3>=4.0
pygeohash>=3.0

# Data Engineering Experiments
duckdb>=0.10.0

# Machine Learning Layer
torch>=2.0.0
mlflow>=2.10.0
pandas>=2.0.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0
pytest>=7.0.0
httpx>=0.24.0
