# Flight Telemetry Platform — Python dependencies
# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Schema validation
jsonschema>=4.20

# Spatial indexing (bronze-to-silver transform)
h3>=4.0
pygeohash>=3.0

# Data Engineering Experiments
duckdb>=0.10.0

# Machine Learning Layer
torch>=2.0.0
mlflow>=2.10.0
pandas>=2.0.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0
pytest>=7.0.0
httpx>=0.24.0

# Serving & API (ML layer — imported by ml/serve.py)
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0

# Postgres / PostGIS access (C2 contract — shared/store/pg.py)
psycopg[binary]>=3.1

# Observability (C6 contract — shared/obs/telemetry.py)
prometheus-client>=0.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-instrumentation-fastapi>=0.45b0

# Drift monitoring (ml/drift.py — ml-08, W4.2)
evidently>=0.7.21

# Dashboard (ws5-01, W4.5) — Streamlit UI
# streamlit supports Python 3.12+ including 3.14; no version marker needed.
streamlit>=1.30

# dbt (ds-06, W4.3) — BigQuery incremental models
# dbt-core/dbt-bigquery/dbt-duckdb have no Python 3.14 wheels (mashumaro
# dependency). The markers below make this file safe to install on Python 3.14
# (packages are silently skipped). dbt runs in CI (Python 3.12) and any
# dedicated 3.10–3.13 venv. dbt-bigquery pins a compatible dbt-core version.
dbt-core>=1.8,<2.0 ; python_version < "3.14"
dbt-bigquery>=1.8,<2.0 ; python_version < "3.14"
# dbt-duckdb: required for offline native unit test execution (--target duckdb_unit).
# dbt unit tests use DuckDB locally; the duckdb_unit profile avoids the
# BigQuery adapter's mandatory relation-cache call that requires live credentials.
dbt-duckdb>=1.8,<2.0 ; python_version < "3.14"
