"""AI layer: eval-driven operational tooling over the gold tables and corpus.

W3.1 builds the *evaluation surface* — a structured analytics tool, a retrieval
tool, a golden question set, and deterministic checks — with no LLM calls. The
answer path and model strategies arrive in W3.2 / W3.3.
"""
